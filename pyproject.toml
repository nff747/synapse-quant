[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "synapse-quant"
version = "0.1.0"
dependencies = ["numpy"]

[tool.pytest.ini_options]
addopts = "-v"
